---
const jobs = [
  {
    title: "Software Developer Intern",
    company: "CodeArts Solutions S.L.",
    date: "March 2025 - Present",
    description: "Currently completing my DAM internship at CodeArts Solutions S.L., gaining experience in cross-platform development."
  }
];
---

<section class="relative max-w-3xl px-6 ml-0">
  {jobs.map((job) => (
    <div class="bg-gray-900 p-6 rounded-lg shadow-lg border border-violet-500/30 mb-6 transition-all hover:shadow-violet-500/50 hover:border-violet-500 animate-fade-in">
      <h3 class="text-white font-bold text-lg">{job.title}</h3>
      <p class="text-violet-400 font-medium">{job.company} - {job.date}</p>
      <p class="text-gray-300 text-sm mt-2">{job.description}</p>
    </div>
  ))}
</section>

<style>
@keyframes fade-in {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}
.animate-fade-in {
  animation: fade-in 0.5s ease-out;
}
</style>





























